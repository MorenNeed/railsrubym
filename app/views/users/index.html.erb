<% if user_signed_in? %>
    <% if current_user.role == "ADMIN" %>
    <%= react_component("users/Index", {signed: true, current_user: current_user, users: @users}) %>
    <% end %>
<% else %>
    <%= controller.redirect_back_or_to '/' %>
<% end%>
